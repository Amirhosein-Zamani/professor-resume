declare module "moment-jalaali" {
  // This package exposes CommonJS types via `export =`.
  // eslint-disable-next-line @typescript-eslint/no-require-imports
  import moment = require("moment");
  export = moment;
}
